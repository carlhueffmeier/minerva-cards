const initialState = {
  byId: {
    basic: {
      id: `basic`,
      name: `Basic`,
      fields: [`Question`, `Answer`],
      templates: [`basic-standard`, `basic-reverse`]
    }
  },
  allIds: [`basic`]
};

export default initialState;
